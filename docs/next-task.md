# Next Task

## Current task

Evaluate real Future Self output quality after:

* naming enforcement
* dominant theme pair distinctness
* proportional chosen-path boosting

All three are now implemented and code-enforced (see
`future-selves-status.md`). Before building any further code-side rules,
evaluate how real model output actually behaves now that these guardrails
are in place — are they producing the intended effect in practice, or
surfacing new failure modes the unit tests (built on synthetic drafts) can't
see?

This is an evaluation pass, not an implementation pass.
